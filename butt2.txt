      bottomNavigationBar: BottomAppBar(
        shape: const CircularNotchedRectangle(),
        notchMargin: 10,
        child: SizedBox(
          height: 60,
          child: Row(
            mainAxisAlignment: MainAxisAlignment.spaceBetween,
            children: <Widget>[
              Row(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  //ECG BUTTON
                  MaterialButton(
                    minWidth: 40,
                    onPressed: (){
                      setState(() {
                        currerntScreen = const ECG();
                        currentIndex = 0;
                      });
                    },
                    child: Column(
                      mainAxisAlignment: MainAxisAlignment.center,
                      children: [
                        Icon(Icons.monitor_heart, color: currentIndex == 0 ? Colors.blue : Colors.grey
                        ),
                        Text('ECG', style: TextStyle(color: currentIndex == 0 ? Colors.blue : Colors.grey
                        ),
                        ),
                      ],
                    ),
                  ),
                  // VITAL BUTTON
                  MaterialButton(
                    minWidth: 40,
                    onPressed: (){
                      setState(() {
                        currerntScreen = const Vital();
                        currentIndex = 1;
                      });
                    },
                    child: Column(
                      mainAxisAlignment: MainAxisAlignment.center,
                      children: [
                        Icon(Icons.accessibility_new, color: currentIndex == 1 ? Colors.blue : Colors.grey
                        ),
                        Text('Vital Signs', style: TextStyle(color: currentIndex == 1 ? Colors.blue : Colors.grey
                        ),
                        ),
                      ],
                    ),
                  ),
                  // SCENERY
                  MaterialButton(
                    minWidth: 40,
                    onPressed: (){
                      setState(() {
                        currerntScreen = const Scenery();
                        currentIndex = 2;
                      });
                    },
                    child: Column(
                      mainAxisAlignment: MainAxisAlignment.center,
                      children: [
                        Icon(Icons.tag, color: currentIndex == 2 ? Colors.blue : Colors.grey
                        ),
                        Text('Scenery', style: TextStyle(color: currentIndex == 2 ? Colors.blue : Colors.grey
                        ),
                        ),
                      ],
                    ),
                  ),
                  // HISTORY
                  MaterialButton(
                    minWidth: 40,
                    onPressed: (){
                      setState(() {
                        currerntScreen = const History();
                        currentIndex = 3;
                      });
                    },
                    child: Column(
                      mainAxisAlignment: MainAxisAlignment.center,
                      children: [
                        Icon(Icons.history, color: currentIndex == 3 ? Colors.blue : Colors.grey
                        ),
                        Text('History', style: TextStyle(color: currentIndex == 3 ? Colors.blue : Colors.grey
                        ),
                        ),
                      ],
                    ),
                  ),
                ],
              )
            ],
          ),
        ),
      ),